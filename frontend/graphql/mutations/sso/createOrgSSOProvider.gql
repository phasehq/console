mutation CreateOrgSSOProvider(
  $orgId: ID!
  $providerType: String!
  $name: String!
  $config: JSONString!
) {
  createOrganisationSsoProvider(
    orgId: $orgId
    providerType: $providerType
    name: $name
    config: $config
  ) {
    providerId
  }
}
