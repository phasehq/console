mutation CreateRole(
  $name: String!
  $description: String!
  $permissions: JSONString!
  $organisationId: ID!
) {
  createCustomRole(
    name: $name
    description: $description
    permissions: $permissions
    organisationId: $organisationId
  ) {
    role {
      id
    }
  }
}
