mutation ResumeRotatingSecretOP($rotatingSecretId: ID!) {
  resumeRotatingSecret(rotatingSecretId: $rotatingSecretId) {
    rotatingSecret {
      id
      isActive
      health
      nextRotationAt
    }
  }
}
