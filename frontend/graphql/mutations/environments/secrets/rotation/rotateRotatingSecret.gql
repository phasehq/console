mutation RotateRotatingSecretOP($rotatingSecretId: ID!) {
  rotateRotatingSecret(rotatingSecretId: $rotatingSecretId) {
    rotatingSecret {
      id
      health
      nextRotationAt
      activeCredential {
        id
        status
        createdAt
      }
    }
  }
}
