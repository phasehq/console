mutation CreateApplication(
  $id: ID!
  $organisationId: ID!
  $name: String!
) {
  createApp(
    id: $id
    organisationId: $organisationId
    name: $name
  ) {
    app {
      id
      name
    }
  }
}
