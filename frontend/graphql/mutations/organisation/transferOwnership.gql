mutation TransferOrgOwnership($organisationId: ID!, $newOwnerId: ID!, $billingEmail: String) {
  transferOrganisationOwnership(organisationId: $organisationId, newOwnerId: $newOwnerId, billingEmail: $billingEmail) {
    ok
  }
}
