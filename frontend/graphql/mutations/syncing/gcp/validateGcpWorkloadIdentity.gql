mutation ValidateGcpWorkloadIdentity($organisationId: ID!, $credentials: JSONString!) {
  validateGcpWorkloadIdentity(organisationId: $organisationId, credentials: $credentials) {
    valid
    error
  }
}
