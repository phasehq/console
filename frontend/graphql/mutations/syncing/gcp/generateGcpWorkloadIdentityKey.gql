mutation GenerateGcpWorkloadIdentityKey($organisationId: ID!) {
  generateGcpWorkloadIdentityKey(organisationId: $organisationId) {
    key {
      issuer
      subject
      keyId
      jwks
      sealedIdentity
    }
  }
}
