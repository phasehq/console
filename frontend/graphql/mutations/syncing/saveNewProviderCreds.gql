mutation SaveNewProviderCreds($orgId: ID!, $provider: String!, $name: String!, $credentials: JSONString!) {
  createProviderCredentials(orgId: $orgId, provider: $provider, name: $name, credentials: $credentials) {
    credential {
      id
    }
  }
}
