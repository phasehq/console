mutation ValidateRotationCredentialsOP(
  $organisationId: ID!
  $providerId: String!
  $credentials: JSONString!
) {
  validateRotationCredentials(
    organisationId: $organisationId
    providerId: $providerId
    credentials: $credentials
  ) {
    valid
    error
  }
}
