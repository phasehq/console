query GetSavedCredentials($orgId: ID!) {
  savedCredentials(orgId: $orgId) {
    id
    name
    revision
    createdAt
    updatedAt
    provider {
      id
      name
    }
    syncCount
    agentConnectionCount
  }
}
