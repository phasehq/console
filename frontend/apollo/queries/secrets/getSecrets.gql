query GetSecrets($envId: ID!) {
  secrets(envId: $envId) {
    id
    key
    value
    createdAt
    history {
      id
      key
      value
      timestamp
      eventType
    }
  }
  environmentKeys(environmentId: $envId) {
    id
    identityKey
    wrappedSeed
    wrappedSalt
  }
}
